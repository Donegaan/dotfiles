- defaults:
    link:
      create: true
      relink: true
      force: true

- clean: ['~']

- link:
    ~/.bash_profile: bash_profile
    ~/.bashrc: bashrc
    ~/.bash_prompt: bash_prompt
    ~/.aliases: aliases
    ~/.gitconfig: gitconfig
    ~/.Library/Application Support/Code/User/settings.json : vscode_settings


